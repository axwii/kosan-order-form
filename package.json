{
  "name": "kosan-order-form",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm install --legacy-peer-deps && next dev --turbopack",
    "build": "npm install --legacy-peer-deps && next build",
    "start": "npm install --legacy-peer-deps && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.24",
    "nodemailer": "^6.9.16",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^5.3.0",
    "react-slick": "^0.30.3",
    "slick-carousel": "^1.8.1"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.17"
  },
  "installCommand": "npm install --legacy-peer-deps"
}
